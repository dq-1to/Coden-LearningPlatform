## リストレンダリング

配列データを一覧表示するには、`map()`メソッドを使います。
各要素にはユニークな`key`属性が必要です。

## 基本的な書き方

```jsx
{items.map(item => (
    <li key={item.id}>{item.name}</li>
))}
```

## keyが必要な理由

Reactが各要素を識別し、効率的に更新するために必要です。
インデックスではなく、ユニークなIDを使いましょう。
