## useEffect とは？

コンポーネントを外部システムと同期させるためのフックです。
- APIからデータを取得
- タイマーの設定
- DOMの直接操作

などの「副作用」を扱います。

## 基本的な使い方

```jsx
useEffect(() => {
    // 副作用の処理
    return () => {
        // クリーンアップ
    };
}, [依存配列]);
```

## 依存配列のパターン

- `[]`: マウント時のみ実行
- `[value]`: valueが変わるたび実行
- 省略: 毎回実行（非推奨）
